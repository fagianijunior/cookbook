<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h5>Ola <%= "#{@user.first_name} #{@user.last_name}" %>, </h5>
    <p>
      Bem vindo ao <%= image_tag("#{ENV['COOKBOOK_HOST']}/assets/logo200x60.png") %>
    </p>
    <p>
      Cadastro realizado com sucesso.<br />
      O seu nome de usuário é: <%= @user.email %><br />
      Clique <%= link_to 'aqui', account_confirmation_url(token: @user.password_reset_token) %> para confirmar o seu email.
    </p>
    <br /><br />
    <p>
      A equipe do Bem vindo ao <%= image_tag("#{ENV['COOKBOOK_HOST']}/assets/logo200x60.png") %> agredece o seu cadastro.
    </p>
  </body>
</html>
