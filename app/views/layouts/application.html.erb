<!DOCTYPE html>
<html>
  <head>
    <title>Cookbook</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Rede social para quem gosta de culinário.">
    <meta name="author" content="Carlos Fagiani Junior">
    <%= stylesheet_link_tag    'application', media: 'all'%>
    <!-- %= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true % -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
		<div class="navbar-header">
  	  <%= link_to image_tag("logo.png", alt: "Cookbook", width: "115"), root_path, class: "navbar-brand" %>
		</div>
		<% if logged_in? %>
      <%= render "layouts/header_logged" %>
    <% else %>
      <%= render "sessions/welcome_new" %>
    <% end %>
	</nav>

  <div id="wrapper">
    <div id="page-content-wrapper">
      <div class="container-fluid">
        <div class="row">
          <div class="col-xs-12 col-mg-12 col-sm-12 col-lg-12">
            <%= render "layouts/flash" %>
          </div>
          <div class="col-xs-12 col-mg-12 col-sm-12 col-lg-12">
            <%= yield %>
            <%= debug(params) if Rails.env.development? %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    $(".btn-close").click(function(e) {
        e.preventDefault();
        $(this).parent().parent().parent().parent().fadeOut();
    });
    
    $("#menu-toggle").click(function(e) {
        e.preventDefault();
        $("#wrapper").toggleClass("toggled");
    });
    
    
    $('textarea.expand').focus(function () {
      $(this).animate({ height: "200px" }, 500);
      $('.btn-post').show(500);
    });

    $('textarea.expand').focusout(function () {
      if ($(this).val().length === 0) {
        $(this).animate({ height: "25px" }, 500);
        $('.btn-post').hide(500);
      }
    });
  </script>

</body>
</html>