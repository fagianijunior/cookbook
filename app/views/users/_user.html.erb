<div class="media well">
  <%= display_small_avatar(user, "pull-left") %>
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to "#{user.first_name} #{user.last_name}", user %>
    </h4>
    <%= "algo mais aqui" %>
  </div>
</div>